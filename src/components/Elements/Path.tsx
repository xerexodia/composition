import React from "react";
import { RGBAColor, Stroke } from "../../../types";

const Path = ({
  x,
  y,
  stroke,
  fill,
  points,
}: {
  x: number;
  y: number;
  stroke: Stroke;
  fill: RGBAColor;
  points: number[][];
}) => {
  return <path></path>;
};

export default Path;
